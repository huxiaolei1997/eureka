eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    # 禁止向注册中心注册自己
    register-with-eureka: false
#  instance:
#    hostname: server2
  # server 端配置关闭自我保护
  server:
    enable-self-preservation: false
server:
  port: 8762